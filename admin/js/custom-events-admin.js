(function( $ ) {
	'use strict';
	/*
	$('#description').on('input', function(){
		var desc = $(this).val();
		console.log(desc);
		if(desc.trim().split(/\s+/).length<=5){
			$(this).addClass('is-invalid')
			$('#descError').text('The description must be atleast 6 words long.')
		}
		else{
			$(this).removeClass('is-invalid')
			$('#descError').text('')
		}
	})
	$('#publish').on('click', function(e){
		e.preventDefault();
		var err = $('.is-invalid')
		if(err.length == 0) {
			$(this).unbind(e)
			$(this).click()
		}
	})*/	

})( jQuery );
